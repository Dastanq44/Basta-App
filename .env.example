# Basta — environment template. Copy to `.env` (which is gitignored) and fill in.
# Only PUBLIC values belong in the app bundle. Never put the service-role key here.
# See DECISIONS.md D-001 and BUGS_AND_WARNINGS.md W-005.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
