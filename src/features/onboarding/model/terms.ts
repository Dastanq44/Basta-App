// Versioned terms acceptance (T-021). When this constant changes, every existing user is
// gated back through profile-setup until they accept the new version. Bump only when the
// terms text actually changes.
export const CURRENT_TERMS_VERSION = '2026-05-28';
