import { Screen, Text } from '@/shared/ui';

// Today tab: due tasks + in-group activity feed (Phase 3). Shell placeholder for now.
export default function TodayScreen() {
  return (
    <Screen>
      <Text variant="title">Today</Text>
      <Text variant="muted">Your tasks and group activity will appear here.</Text>
    </Screen>
  );
}
